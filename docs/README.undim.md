# window-tool undim

Remove the active dim overlay.

## Usage

```sh
window-tool undim
```

## Details

Terminates the running dim overlay process. Errors if no dim overlay is active.

## Examples

```sh
window-tool undim
```
